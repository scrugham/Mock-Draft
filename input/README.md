# Input (not in git)

Place your Zite export here as **`Entries - Grid view.csv`** (exact filename expected by `npm run prebuild`).

That file is **gitignored** so names, emails, and picks never go to public GitHub. Run `npm run prebuild` from the repo root to generate `public/entries.json`, which is also gitignored.
